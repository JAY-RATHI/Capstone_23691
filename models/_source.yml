version: 2

sources:
  - name: external
    database: CT_JAY_RATHI_DB
    schema: EXTERNAL

    config:
      loaded_at_field: FILE_LAST_MODIFIED
      freshness:
        warn_after:
          count: 12
          period: day

    tables:
      - name: campaign
        identifier: ext_campaign
      - name: customer
        identifier: ext_customer
      - name: employee
        identifier: ext_employee
      - name: orders
        identifier: ext_orders
      - name: product
        identifier: ext_product
      - name: store
        identifier: ext_store
      - name: supplier
        identifier: ext_supplier